# Decision_Tree_classifier
This project implements a Decision Tree classifier for customer churn prediction. It preprocesses the dataset, evaluates different tree depths using repeated stratified cross-validation, selects the best model, and visualizes the decision tree, decision boundaries, accuracy distribution, and confusion matrix.
